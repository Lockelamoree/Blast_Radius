# Judge Review 000 — Baseline pre-mortem (2026-07-13)

**Build stage:** concept-docs-only (start of the schedule; no product code yet).
**Focus:** concept + positioning pre-mortem + harness validation.
**Run:** original five-perspective planning review with a Stage-One gate and correctness/vocabulary audit.

> This baseline judges the *plan and positioning* (there is no product yet). Its purpose: validate the harness and stress-test the concept. Re-run after each build day for a real delta.

## Stage-One kill gate: ❌ FAIL (dead-at-gate — expected at this stage)
Fails ~7 of 8 lines because nothing is built yet: not a git repo, no LICENSE, no product README, no real `AGENTS.md`, no engine/server/ui, GPT-5.6 not wired into any runtime, no hosted demo, no video, `/feedback` Session ID is a placeholder. **This is normal on Day 0** — the point is the trajectory.

## Scorecard (against the *current* state)
| Criterion | Score /10 |
|---|---|
| Technological Implementation *(tiebreaker)* | **2** |
| Design | **2** |
| Potential Impact | **3** |
| Quality of the Idea | **8** |

| Judge | /10 | One-line |
|---|---|---|
| Sottiaux | 3 | Excellent idea, self-aware plan — but on disk it's a plan, not a product. |
| Korevec | 3 | Sharpest crit-is-the-product concept in the track; zero dogfooding yet; fabricated stat. |
| Seshan | 3.5 | Most on-brand concept for my taste; not one runnable pixel exists. |
| Belsky | 3 | Best-articulated learning design; the winning number (2/5→5/5) is pre-written, not measured. |
| Steinberger | 3 | Best battle plan all week — and nothing to play. I score demos, not decks. |

**Overall (current state): ~38/100. Predicted standing: bottom-tier / non-advancing *today*.** But Quality of Idea (8/10) is genuinely top-quartile — the concept will carry this into contention the moment an honest vertical slice exists. **Trajectory, not current state, is the story.**

**Weakest link:** Technological Implementation (2/10) — the tiebreaker, and the thing the whole pitch rests on. No Codex thread, no GPT-5.6 in any runtime, no self-verifying loop. Nothing else moves until this is non-zero.

## Fatal flaws (and the fix)
1. **Zero product code.** Not a git repo; no engine/server/ui; the only runnable code is the judge harness itself. → Ship the smallest honest vertical slice (git init + LICENSE + product README + one deployed GPT-5.6-graded round + the correctness gate over a real fallback bank). Drop generator/adversary/fold-ins for now.
2. **Fabricated-in-advance claims (integrity — P0).** `06_DEVPOST` + `04_PITCH` asserted a *completed* Codex build and a *measured* "2/5→5/5" that never happened. For a panel whose trust test is "do the receipts check out," a fabricated receipt in the submission is disqualifying. → **FIXED 2026-07-13:** past-tense/measured claims converted to labeled placeholders + a DRAFT banner; real numbers to be filled only after measurement.
3. **Correctness-gate integrity is vacuous.** The load-bearing rule "never show an ungated scenario" is enforced by nothing on disk. → Build the correctness spine FIRST: a committed scenario bank + the GPT-5.6 critic gate + the mandatory negative test (feed a known-bad exploit, assert rejection).

## Ranked punch-list (for Codex — score-per-hour order)
- **P0 · #1 — Real repo:** `git init`, MIT LICENSE, `.gitignore` (secrets/.env), first commit; small story-telling commits thereafter.
- **P0 · #2 — Integrity sweep (✅ done for the copy today):** no past-tense build claims, no unmeasured numbers. Keep it that way.
- **P0 · #3 — Correctness spine:** `engine/bank/scenarios.json` (15–20 verified scenarios w/ ground-truth receipts) + GPT-5.6 critic gate (Structured Outputs) + negative test asserting rejection of a planted hallucinated exploit.
- **P0 · #4 — One hosted browser round:** pick APPROVE/SANDBOX/REJECT + type a reason → GPT-5.6 grades the *reasoning* vs ground truth → receipt in <3s, off the fallback bank; deploy to a public URL; add install/platforms.
- **P0 · #5 — Real Codex evidence:** primary thread w/ `/plan`+`/review`; capture `/feedback` Session ID; promote `AGENTS.md` (root+nested) and the `verify-scenario` Skill from templates into real files.
- **P1 · #6 — One real pre/post delta** with a named developer, screen-recorded; fill the placeholder with the true number.
- **P1 · #7 — The <3:00 video** showing BOTH techs + the gate rejecting a planted exploit; ≤1 menace beat.
- **P2 · #8 — Cut & de-menace:** defer generator/adversary/MCP-inception/both fold-ins until the slice is stable; trim dread phrasing to one beat per artifact; keep "compliance" out of the public README.

**Next review:** re-run after Jul 14 (correctness spine) — expect Technological Implementation to jump from 2 toward 6–8 once the gate + one graded round exist.
